import styled from 'styled-components';

const Button = styled.button`
    color: white;
    border-radius: 20px;
    cursor: pointer;
    transition: 0.5s;
    padding: 5px 30px;
   background: red;
  &:hover {
    box-shadow: none;
  }
`

export default Button